import { HghlightDirective } from './hghlight.directive';

describe('HghlightDirective', () => {
  it('should create an instance', () => {
    const directive = new HghlightDirective();
    expect(directive).toBeTruthy();
  });
});
